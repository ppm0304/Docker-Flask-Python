git clone https://github.com/lvthillo/python-flask-docker.git (how to copy or clone a repositoty from github account)
sudo docker build -t pfd-image .
sudo docker run -d -p 8080:8080 --name pfd-container pfd-image
